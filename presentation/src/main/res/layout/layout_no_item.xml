<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center">

    <ImageView
        android:id="@+id/ivImage"
        android:layout_width="@dimen/no_item_icon_size"
        android:layout_height="@dimen/no_item_icon_size"
        tools:src="@drawable/ic_tomato" />

    <TextView
        android:id="@+id/tvContent"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginStart="@dimen/common_horizontal_margin"
        android:layout_marginEnd="@dimen/common_horizontal_margin"
        android:layout_marginTop="@dimen/no_item_margin_top"
        android:textSize="@dimen/no_item_content_text_size"
        android:gravity="center_horizontal"
        android:fontFamily="sans-serif-medium"
        android:includeFontPadding="false"
        tools:text="내용" />

    <Button
        style="@style/baseButton"
        android:id="@+id/btHandling"
        tools:text="버튼"
        android:layout_marginTop="@dimen/no_item_margin_top" />
</LinearLayout>
